const express = require('express');
const router = express.Router();

let user = {username:bernadette, password:password1};

app.get('/', function(req, res){
  res.render("   ")
})

module.exports = router;
